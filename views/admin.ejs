<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>User Information</title>
</head>

<body>
    <div class="container">
        <%- include('menu.ejs') %>
        <% if (userIsAdmin) { %>
            <h1>Пользователи</h1>
						<a href="/users/createUser">Добавить нового пользователя</a>
            <table>
                <tr>
                    <th>Логин</th>
                    <th>Имя</th>
                    <th>Телефон</th>
                    <th>Telegram</th>
                    <th>Всего словарей</th>
                    <th>Всего слов</th>
                    <th>Действие</th>
                </tr>
                <% users.forEach(user => { %>
                    <% let id = user._id %>
                    <% let countDictionaries = 0 %>
                    <% let countWords = 0 %>
                    <% for (const iterator of dictionaries) { %>
                        <% if (iterator.user == id) { %>
                            <% countDictionaries += 1 %>
                            <% countWords += iterator.words.length %>
                        <% } %>
                    <% } %>
                    <tr>
                        <td><%= user.userlogin %></td>
                        <td><%= user.username %></td>
                        <td><%= user.contactinfo.phoneNumber %></td>
                        <td><%= user.contactinfo.telegramm %></td>
                        <td><%= countDictionaries %></td>
                        <td><%= countWords %></td>
                        <td>
                            <a href="/users/editUser/<%= user._id %>">Редактировать</a>
                            <form action="/users/deleteUser/<%= user._id %>" method="post" onsubmit="return confirm('Вы уверены?')">
                                <button type="submit">Удалить</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </table>
        <% } %>
    </div>
</body>

</html>
