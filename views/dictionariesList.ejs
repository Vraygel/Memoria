<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description"
        content="Сайт для интервального повторения информации. Поможет вам эффективно запоминать любую информацию и иностранные слова.">
    <meta name="keywords"
        content="интервальное повторение, запоминание информации, запоминание слов, запоминание иностранных слов, обучение, эффективное обучение, обучение с помощью интервалов, обучение с повторениями">
    <meta name="author" content="Райгель Владимир Александрович VRaygel">
    <link rel="icon" href="./favicon.png" type="image/png">
    <link rel="stylesheet" href="/css/style.css">
    <title>Memboost <%= dictionary.name %>
    </title>
</head>

<body>
    <%- include('template.ejs') %>
        <%- include('menu.ejs') %>
            <div class="container">
                <div class="wrapper">

                        

                        <div class="purchase">

                            <form class="deleteProfil" action="/words/addWordPage/<%= dictionary._id %>" method="GET">
                                <!-- <a type="submit">Удалить профиль</a> -->
                                <button class="delete-button" type="submit"><a>Новый термин</a></button>
                            </form>

                            <form class="deleteProfil" action="/study/words/<%= dictionary._id %>" method="GET">
                                <!-- <a type="submit">Удалить профиль</a> -->
                                <button class="delete-button" type="submit"><a>Учить термины</a></button>
                            </form>

                        <form class="deleteProfil" action="/study/repetition" method="GET">
                            <!-- <a type="submit">Удалить профиль</a> -->
                            <button class="delete-button" type="submit"><a>Повторять термины</a></button>
                        </form>
                        
                    </div>

                    <h1>Термины: <%= dictionary.name %></h1>

                    <!-- <div class="dictinaries_menu">
                        <a href="/words/addWordPage/<%= dictionary._id %>"><button>Создать</button></a>
                        <a href="/study/words/<%= dictionary._id %>"><button>Учить</button></a>
                        <a href="/study/repetition"><button>Повторять</button></a>
                    </div> -->

                    <!-- <div class="purchase">
                        <form class="deleteProfil" action="/dictionaries/editDictionary/<%= dictionary._id %>"
                            method="GET">
                            <a type="submit">Удалить профиль</a>
                            <button class="delete-button" type="submit"><a>Редактировать</a></button>
                        </form>
                        <form class="deleteProfil" action="/dictionaries/deleteDictionary/<%= dictionary._id %>"
                            method="POST" onsubmit="return confirm('Вы уверены, что хотите удалить этот раздел?')">
                            <a type="submit">Удалить профиль</a>
                            <button class="delete-button" type="submit"><a>Удалить раздел</a></button>
                        </form>
                    </div>
                    <h1>
                        <%= dictionary.name %>
                    </h1> -->

                    <% if (dictionary.words.length> 0) { %>
                        <% dictionary.words.forEach(word=> { %>

                            <div class="dictionaryList_wrap">
                                <%= word.enum %>
                                <%= word.waitingTime %>
                                <div class="purchase">
                                    <form class="deleteProfil" action="/words/editWord/<%= word._id %>?dictionaryId=<%= dictionary._id %>"
                                        method="GET">
                                        <input type="hidden" name="dictionaryId" value="<%= dictionary._id %>">
                                        <button class="delete-button newDictonaries_button" type="submit"><a>Редактировать</a></button>
                                    </form>
                                    <form class="deleteProfil" action="/words/deleteWord/<%= word._id %>" method="POST"
                                        onsubmit="return confirm('Вы уверены?')">
                                        <button class="delete-button" type="submit"><a>Удалить термин</a></button>
                                    </form>
                                </div>

                                <div class="dictionaryList_item" style="white-space: pre-line;">
                                    <p>
                                        <%= word.word %>
                                        <span class="sndon" onclick="soundClick('/uploads/<%= word.wordFileOriginalname %>')"></span>
                                        <audio controls src="/uploads/<%= word.wordFileOriginalname %>"></audio>
                                        <audio controls>
                                            <source src="/uploads/<%= word.wordFileOriginalname %>" type="audio/mp3">
                                            Your browser does not support the audio element.
                                        </audio>
                                        



                                        
                                        
                                    </p>
                                </div>

                                <div class="dictionaryList_item" style="white-space: pre-line;">
                                    <p>
                                        <%= word.translation %>
                                    </p>
                                </div>


                                

                            </div>

                            <% }); %>
                                <!-- <table>
                            <tbody>
                                <% dictionary.words.forEach(word=> { %>
                                    


                                    <tr>
                                        <td class="dictionaryList_td">
                                            <%= word.word %>
                                        </td>
                                        <td class="dictionaryList_td">
                                            <%= word.translation %>
                                        </td>
                                        <td class="td_editWord dictionaryList_td">
                                            <% if(word.enum !='new' ) { %>

                                                <% } else {%>
                                                    <% } %>
                                                        <form
                                                            action="/words/editWord/<%= word._id %>?dictionaryId=<%= dictionary._id %>"
                                                            method="GET">
                                                            <input type="hidden" name="dictionaryId"
                                                                value="<%= dictionary._id %>">
                                                            <button class="edit-button"
                                                                type="submit">Редактировать</button>
                                                        </form>
                                                        <form action="/words/deleteWord/<%= word._id %>" method="POST"
                                                            onsubmit="return confirm('Вы уверены?')">
                                                            <button class="edit-button" type="submit">Удалить</button>
                                                        </form>
                                        </td>
                                    </tr>
                                    <% }); %>
                            </tbody>
                        </table> -->
                                <% } else { %>
                                    <p>В этом разделе нет терминов.</p>
                                    <% } %>
                </div>
            </div>
            <%- include('footer.ejs') %>
            <script>
function soundClick(url) {
  var audio = new Audio(); // Создаём новый элемент Audio
  audio.src = url; // Указываем путь к звуку "клика"
  audio.autoplay = true; // Автоматически запускаем
}

            </script>
</body>

</html>