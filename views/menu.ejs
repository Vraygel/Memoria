<nav class="menu">

	<% if (typeof user==='undefined' ) { %>
		<% var user=false; %>
			<% } %>
					<a href="/index"><button>Главная</button></a>
				<% if (user) { %>
					<a href="/dictionaries"><button>Разделы</button></a>
					<a href="/user/profile"><button>Профиль</button></a>
					
					<% if (user.userstatus=='admin' ) { %>
						<a href="/admin"><button>Админ</button></a>
						<% } %>
							<% } else { %>
								<a href="/auth/login"><button>Вход</button></a>
								<a href="/auth/register"><button>Регистрация</button></a>
								<% } %>
</nav>